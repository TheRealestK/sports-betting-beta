services:
  - type: web
    name: sports-betting-beta
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python simple_beta.py"
    envVars:
      - key: PYTHON_VERSION
        value: "3.11"
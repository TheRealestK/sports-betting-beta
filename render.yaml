services:
  - type: web
    name: sports-betting-beta
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python beta_platform.py"
    envVars:
      - key: PYTHON_VERSION
        value: "3.11.10"
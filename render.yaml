services:
  - type: web
    name: sports-betting-beta
    env: python
    buildCommand: "echo 'No build needed for minimal server'"
    startCommand: "python ultra_simple.py"
    envVars:
      - key: PYTHON_VERSION
        value: "3.11"